/* ?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧
   THE PANG ??Admin Panel CSS
   ?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧 */

@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');

:root {
    --bg-base:       #f5f5f7;
    --bg-surface:    #ffffff;
    --bg-panel:      #f0f0f4;
    --bg-hover:      #e8e8f0;
    --border:        rgba(0,0,0,0.10);
    --border-focus:  rgba(123,47,255,0.7);
    --purple:        #7b2fff;
    --purple-light:  #9a5cff;
    --orange:        #ff5e00;
    --red:           #e63946;
    --green:         #16a34a;
    --text-primary:  #18181b;
    --text-secondary:#52525b;
    --text-muted:    #a1a1aa;
    --radius-sm:     8px;
    --radius-md:     12px;
    --radius-lg:     16px;
    --shadow:        0 4px 24px rgba(0,0,0,0.08);
    --shadow-purple: 0 4px 24px rgba(123,47,255,0.25);
    --transition:    0.2s ease;
    --sidebar-width: 240px;
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

body {
    font-family: 'Inter', sans-serif;
    background: var(--bg-base);
    color: var(--text-primary);
    font-size: 14px;
    line-height: 1.5;
    min-height: 100vh;
}

a { color: inherit; text-decoration: none; }
button { cursor: pointer; font-family: inherit; border: none; }
input, textarea, select { font-family: inherit; }

/* ?? Scrollbar ?? */
::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: var(--bg-panel); }
::-webkit-scrollbar-thumb { background: rgba(0,0,0,0.18); border-radius: 3px; }

/* ?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧
   LOGIN PAGE
   ?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧 */
.login-page {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    background: radial-gradient(ellipse at 30% 40%, rgba(123,47,255,0.07) 0%, transparent 60%),
                radial-gradient(ellipse at 70% 70%, rgba(255,94,0,0.05) 0%, transparent 50%),
                var(--bg-base);
}

.login-card {
    width: 100%;
    max-width: 400px;
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 48px 40px;
    box-shadow: var(--shadow);
}

.login-logo {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 32px;
}

.login-logo__mark {
    width: 40px;
    height: 40px;
    background: linear-gradient(135deg, var(--purple), var(--orange));
    border-radius: var(--radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 18px;
}

.login-logo__text {
    font-size: 18px;
    font-weight: 700;
    letter-spacing: 0.5px;
}

.login-logo__sub {
    font-size: 11px;
    color: var(--text-muted);
    font-weight: 500;
    letter-spacing: 1px;
    text-transform: uppercase;
}

.login-card h1 {
    font-size: 22px;
    font-weight: 700;
    margin-bottom: 8px;
}

.login-card p {
    color: var(--text-secondary);
    font-size: 13px;
    margin-bottom: 32px;
}

.form-group {
    margin-bottom: 16px;
}

.form-group label {
    display: block;
    font-size: 12px;
    font-weight: 600;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 8px;
}

.form-control {
    width: 100%;
    background: var(--bg-panel);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 12px 16px;
    color: var(--text-primary);
    font-size: 14px;
    transition: var(--transition);
    outline: none;
}

.form-control:focus {
    border-color: var(--border-focus);
    box-shadow: 0 0 0 3px rgba(123,47,255,0.12);
}

.btn-primary {
    width: 100%;
    padding: 14px;
    background: var(--purple);
    color: #fff;
    border-radius: var(--radius-sm);
    font-size: 14px;
    font-weight: 600;
    transition: var(--transition);
    margin-top: 8px;
}

.btn-primary:hover {
    background: var(--purple-light);
    box-shadow: var(--shadow-purple);
    transform: translateY(-1px);
}

.login-error {
    background: rgba(230,57,70,0.12);
    border: 1px solid rgba(230,57,70,0.3);
    color: var(--red);
    padding: 10px 14px;
    border-radius: var(--radius-sm);
    font-size: 13px;
    margin-bottom: 16px;
    display: none;
}

.login-error.show { display: block; }

/* ?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧
   DASHBOARD LAYOUT
   ?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧 */
.admin-layout {
    display: flex;
    min-height: 100vh;
}

/* ?? Sidebar ?? */
.sidebar {
    width: var(--sidebar-width);
    background: var(--bg-surface);
    border-right: 1px solid var(--border);
    display: flex;
    flex-direction: column;
    position: fixed;
    top: 0;
    left: 0;
    height: 100vh;
    overflow-y: auto;
    z-index: 100;
}

.sidebar-logo {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 20px 20px 16px;
    border-bottom: 1px solid var(--border);
}

.sidebar-logo__mark {
    width: 32px;
    height: 32px;
    background: linear-gradient(135deg, var(--purple), var(--orange));
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    flex-shrink: 0;
}

.sidebar-logo__name {
    font-size: 14px;
    font-weight: 700;
}

.sidebar-logo__admin {
    font-size: 10px;
    color: var(--text-muted);
    font-weight: 500;
    letter-spacing: 0.5px;
}

.sidebar-nav {
    flex: 1;
    padding: 12px 0;
}

.nav-section {
    padding: 0 12px;
    margin-bottom: 4px;
}

.nav-section-label {
    font-size: 10px;
    font-weight: 600;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 1px;
    padding: 8px 8px 4px;
}

.nav-item {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 9px 10px;
    border-radius: var(--radius-sm);
    color: var(--text-secondary);
    font-size: 13px;
    font-weight: 500;
    transition: var(--transition);
    cursor: pointer;
    user-select: none;
}

.nav-item:hover {
    background: var(--bg-hover);
    color: var(--text-primary);
}

.nav-item.active {
    background: rgba(123,47,255,0.15);
    color: var(--purple-light);
}

.nav-item i {
    font-size: 16px;
    width: 18px;
    flex-shrink: 0;
}

.sidebar-footer {
    padding: 12px;
    border-top: 1px solid var(--border);
}

.sidebar-footer .nav-item {
    color: var(--red);
}

.sidebar-footer .nav-item:hover {
    background: rgba(230,57,70,0.1);
    color: var(--red);
}

/* ?? Main Content ?? */
.admin-main {
    margin-left: var(--sidebar-width);
    flex: 1;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

.admin-topbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 16px 28px;
    background: var(--bg-surface);
    border-bottom: 1px solid var(--border);
    position: sticky;
    top: 0;
    z-index: 50;
}

.topbar-title {
    font-size: 16px;
    font-weight: 600;
}

.topbar-badge {
    background: rgba(123,47,255,0.15);
    color: var(--purple-light);
    border: 1px solid rgba(123,47,255,0.3);
    font-size: 11px;
    font-weight: 600;
    padding: 2px 10px;
    border-radius: 20px;
}

.topbar-actions {
    display: flex;
    align-items: center;
    gap: 10px;
}

.btn-preview {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 8px 16px;
    background: var(--bg-panel);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    color: var(--text-secondary);
    font-size: 13px;
    font-weight: 500;
    transition: var(--transition);
}

.btn-preview:hover {
    border-color: var(--border-focus);
    color: var(--purple-light);
}

.btn-save {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 8px 18px;
    background: var(--purple);
    border-radius: var(--radius-sm);
    color: #fff;
    font-size: 13px;
    font-weight: 600;
    transition: var(--transition);
}

.btn-save:hover {
    background: var(--purple-light);
    box-shadow: var(--shadow-purple);
}

.btn-save.saved {
    background: var(--green);
}

.admin-content {
    flex: 1;
    padding: 28px;
}

/* ?? Dashboard Cards ?? */
.stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    margin-bottom: 28px;
}

.stat-card {
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    padding: 20px;
}

.stat-card__label {
    font-size: 12px;
    color: var(--text-muted);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 8px;
}

.stat-card__value {
    font-size: 28px;
    font-weight: 700;
    color: var(--text-primary);
}

.stat-card__sub {
    font-size: 12px;
    color: var(--green);
    margin-top: 4px;
}

.section-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
}

.section-card {
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    padding: 20px;
    cursor: pointer;
    transition: var(--transition);
}

.section-card:hover {
    border-color: var(--border-focus);
    background: var(--bg-panel);
}

.section-card__icon {
    width: 40px;
    height: 40px;
    border-radius: var(--radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
    margin-bottom: 12px;
}

.section-card__title {
    font-size: 14px;
    font-weight: 600;
    margin-bottom: 4px;
}

.section-card__desc {
    font-size: 12px;
    color: var(--text-muted);
    line-height: 1.5;
}

/* ?? Editor Panels ?? */
.editor-panel {
    display: none;
}

.editor-panel.active {
    display: block;
}

.panel-header {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 24px;
}

.panel-header__back {
    width: 32px;
    height: 32px;
    border-radius: var(--radius-sm);
    background: var(--bg-panel);
    border: 1px solid var(--border);
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    color: var(--text-secondary);
    transition: var(--transition);
}

.panel-header__back:hover {
    color: var(--text-primary);
    border-color: var(--border-focus);
}

.panel-header h2 {
    font-size: 18px;
    font-weight: 600;
}

.editor-card {
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    padding: 24px;
    margin-bottom: 16px;
}

.editor-card__title {
    font-size: 13px;
    font-weight: 600;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 16px;
    padding-bottom: 12px;
    border-bottom: 1px solid var(--border);
    display: flex;
    align-items: center;
    gap: 8px;
}

.editor-row {
    margin-bottom: 16px;
}

.editor-row:last-child {
    margin-bottom: 0;
}

.editor-row label {
    display: block;
    font-size: 12px;
    font-weight: 600;
    color: var(--text-secondary);
    margin-bottom: 6px;
}

textarea.form-control {
    resize: vertical;
    min-height: 80px;
}

/* ?? Image Manager ?? */
.image-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
    gap: 12px;
    margin-bottom: 16px;
}

.image-card {
    aspect-ratio: 9/16;
    border-radius: var(--radius-sm);
    border: 1px solid var(--border);
    overflow: hidden;
    position: relative;
    background: var(--bg-panel);
}

.image-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.image-card__del {
    position: absolute;
    top: 4px;
    right: 4px;
    width: 22px;
    height: 22px;
    background: rgba(230,57,70,0.85);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    opacity: 0;
    transition: var(--transition);
    font-size: 12px;
    color: #fff;
}

.image-card:hover .image-card__del {
    opacity: 1;
}

.image-add-btn {
    aspect-ratio: 9/16;
    border-radius: var(--radius-sm);
    border: 1px dashed var(--border);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 6px;
    cursor: pointer;
    transition: var(--transition);
    color: var(--text-muted);
    font-size: 12px;
    background: var(--bg-panel);
}

.image-add-btn:hover {
    border-color: var(--border-focus);
    color: var(--purple-light);
}

.image-add-btn i {
    font-size: 24px;
}

/* ?? Pricing Editor ?? */
.pricing-editor-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
}

.plan-editor {
    background: var(--bg-panel);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    padding: 20px;
}

.plan-editor__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 16px;
}

.plan-badge {
    font-size: 11px;
    font-weight: 700;
    padding: 2px 10px;
    border-radius: 20px;
    letter-spacing: 0.5px;
}

.plan-badge--starter  { background: rgba(255,255,255,0.1); color: #fff; }
.plan-badge--standard { background: rgba(123,47,255,0.2); color: var(--purple-light); }
.plan-badge--premium  { background: rgba(255,255,255,0.15); color: #fff; }
.plan-badge--enterprise { background: rgba(255,94,0,0.2); color: var(--orange); }

.feature-list-input {
    width: 100%;
    background: var(--bg-surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 10px 14px;
    color: var(--text-primary);
    font-size: 13px;
    resize: vertical;
    min-height: 120px;
    outline: none;
    transition: var(--transition);
}

.feature-list-input:focus {
    border-color: var(--border-focus);
}

/* ?? Testimonial Editor ?? */
.testimonial-editor {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
}

.testimonial-item {
    background: var(--bg-panel);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    padding: 20px;
}

/* ?? Toast ?? */
.toast {
    position: fixed;
    bottom: 24px;
    right: 24px;
    background: var(--bg-panel);
    border: 1px solid var(--green);
    color: var(--green);
    padding: 12px 20px;
    border-radius: var(--radius-sm);
    font-size: 13px;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 8px;
    box-shadow: var(--shadow);
    transform: translateY(80px);
    opacity: 0;
    transition: all 0.3s ease;
    z-index: 9999;
}

.toast.show {
    transform: translateY(0);
    opacity: 1;
}

/* ?? Util ?? */
.tag {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    padding: 3px 10px;
    border-radius: 20px;
    font-size: 11px;
    font-weight: 600;
}

.tag--purple { background: rgba(123,47,255,0.15); color: var(--purple-light); }
.tag--orange { background: rgba(255,94,0,0.15);   color: var(--orange); }
.tag--red    { background: rgba(230,57,70,0.15);   color: var(--red); }
.tag--green  { background: rgba(34,197,94,0.15);   color: var(--green); }

/* ?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧
   MOBILE RESPONSIVE
   ?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧 */

/* ?? ?꾨쾭嫄?踰꾪듉 (湲곕낯 ?④?, 紐⑤컮?쇱뿉?쒕쭔 ?쒖떆) ?? */
.hamburger-btn {
    display: none;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    background: var(--bg-panel);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    color: var(--text-primary);
    font-size: 20px;
    transition: var(--transition);
    flex-shrink: 0;
}

.hamburger-btn:hover {
    border-color: var(--border-focus);
    color: var(--purple);
}

/* ?? ?ъ씠?쒕컮 ?リ린 踰꾪듉 (湲곕낯 ?④?) ?? */
.sidebar-close {
    display: none;
    align-items: center;
    justify-content: center;
    width: 30px;
    height: 30px;
    margin-left: auto;
    background: transparent;
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    color: var(--text-secondary);
    font-size: 18px;
    cursor: pointer;
    transition: var(--transition);
    flex-shrink: 0;
}

.sidebar-close:hover {
    background: var(--bg-hover);
    color: var(--text-primary);
}

/* ?? ?ㅻ쾭?덉씠 (湲곕낯 ?④?) ?? */
.sidebar-overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.35);
    z-index: 99;
    opacity: 0;
    transition: opacity 0.3s ease;
}

.sidebar-overlay.active {
    display: block;
    opacity: 1;
}

/* ?? ?댁퐫?붿뼵 怨듯넻 ?? */
.accordion-trigger {
    cursor: pointer;
    user-select: none;
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.accordion-arrow {
    font-size: 16px;
    transition: transform 0.25s ease;
}

.accordion-section.open .accordion-arrow {
    transform: rotate(180deg);
}

/* PC?먯꽌???댁퐫?붿뼵 body ??긽 ?쒖떆 */
.accordion-body {
    overflow: hidden;
}

/* ?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧
   @media 768px ?댄븯 (紐⑤컮??
   ?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧?먥븧 */
@media (max-width: 768px) {

    /* ?꾨쾭嫄?踰꾪듉 ?쒖떆 */
    .hamburger-btn {
        display: flex;
    }

    /* ?ъ씠?쒕컮 ?リ린 踰꾪듉 ?쒖떆 */
    .sidebar-close {
        display: flex;
    }

    /* ?ъ씠?쒕컮: ?ㅽ봽?ㅽ겕由??щ씪?대뱶 */
    .sidebar {
        position: fixed;
        left: -260px;
        top: 0;
        width: 260px;
        height: 100vh;
        z-index: 100;
        transition: left 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        box-shadow: none;
    }

    .sidebar.open {
        left: 0;
        box-shadow: 4px 0 24px rgba(0,0,0,0.12);
    }

    /* 硫붿씤 ?곸뿭: ?ъ씠?쒕컮 留덉쭊 ?놁쓬 */
    .admin-main {
        margin-left: 0;
    }

    /* ?댁퐫?붿뼵: 紐⑤컮?쇱뿉?쒕뒗 collapsed媛 湲곕낯 */
    .accordion-body {
        max-height: 0;
        transition: max-height 0.3s ease;
    }

    .accordion-section.open .accordion-body {
        max-height: 400px;
    }

    /* ?댁퐫?붿뼵 ?쇰꺼 媛뺤“ */
    .accordion-trigger {
        background: var(--bg-hover);
        border-radius: var(--radius-sm);
        padding: 8px 10px;
        margin-bottom: 2px;
    }

    /* Stats 洹몃━?? 2??*/
    .stats-grid {
        grid-template-columns: repeat(2, 1fr);
    }

    /* Section 移대뱶: 1??*/
    .section-grid {
        grid-template-columns: 1fr;
    }

    /* 媛寃⑺몴 ?먮뵒?? 1??*/
    .pricing-editor-grid {
        grid-template-columns: 1fr;
    }

    /* ?꾧린 ?먮뵒?? 1??*/
    .testimonial-editor {
        grid-template-columns: 1fr;
    }

    /* ?묐컮 ?⑤뵫 以꾩씠湲?*/
    .admin-topbar {
        padding: 12px 16px;
    }

    /* 而⑦뀗痢??⑤뵫 以꾩씠湲?*/
    .admin-content {
        padding: 16px;
    }

    /* 誘몃━蹂닿린 踰꾪듉 ?띿뒪???④린湲?*/
    .btn-preview span,
    .btn-preview i ~ * {
        display: none;
    }
}


/* ??????????????????????????????????????????????????????????
   회원 관리 패널
   ?????????????????????????????????????????????????????????? */

.stat-card--members { border: 2px solid transparent; transition: border-color 0.2s, box-shadow 0.2s; }
.stat-card--members:hover { border-color: var(--green); box-shadow: 0 0 0 3px rgba(22,163,74,0.12); }

.members-toolbar { display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-bottom: 16px; flex-wrap: wrap; }
.members-search { display: flex; align-items: center; gap: 8px; background: var(--bg-surface); border: 1px solid var(--border); border-radius: var(--radius-sm); padding: 8px 14px; flex: 1; min-width: 200px; transition: border-color var(--transition); }
.members-search:focus-within { border-color: var(--border-focus); }
.members-search i { color: var(--text-muted); font-size: 16px; flex-shrink: 0; }
.members-search input { border: none; outline: none; background: transparent; font-size: 14px; color: var(--text-primary); width: 100%; }
.members-search input::placeholder { color: var(--text-muted); }
.members-stats { display: flex; align-items: center; gap: 10px; font-size: 14px; color: var(--text-secondary); white-space: nowrap; }
.members-stats strong { color: var(--text-primary); font-weight: 600; }

.btn-member-refresh { background: var(--bg-panel); border: 1px solid var(--border); border-radius: var(--radius-sm); width: 32px; height: 32px; display: flex; align-items: center; justify-content: center; color: var(--text-secondary); font-size: 16px; transition: background var(--transition), color var(--transition); }
.btn-member-refresh:hover { background: var(--bg-hover); color: var(--purple); }

.members-table-wrap { overflow-x: auto; }
.members-table { width: 100%; border-collapse: collapse; font-size: 14px; }
.members-table thead tr { background: var(--bg-panel); border-bottom: 1px solid var(--border); }
.members-table th { padding: 12px 16px; text-align: left; font-weight: 600; font-size: 12px; color: var(--text-secondary); text-transform: uppercase; letter-spacing: 0.04em; white-space: nowrap; }
.member-num { color: var(--text-muted); width: 40px; }
.members-table tbody .member-row { border-bottom: 1px solid var(--border); transition: background var(--transition); }
.members-table tbody .member-row:last-child { border-bottom: none; }
.members-table tbody .member-row:hover { background: var(--bg-panel); }
.members-table td { padding: 12px 16px; vertical-align: middle; }

.member-name-cell { display: flex; align-items: center; gap: 10px; }
.member-avatar { width: 34px; height: 34px; border-radius: 50%; background: linear-gradient(135deg, var(--purple), var(--orange)); color: #fff; display: flex; align-items: center; justify-content: center; font-weight: 700; font-size: 14px; flex-shrink: 0; }
.member-email { color: var(--text-secondary); font-size: 13px; }

.member-badge { display: inline-flex; align-items: center; padding: 3px 10px; border-radius: 20px; font-size: 12px; font-weight: 500; }
.member-badge--online { background: rgba(22,163,74,0.12); color: var(--green); border: 1px solid rgba(22,163,74,0.25); }
.member-badge--normal { background: var(--bg-panel); color: var(--text-muted); border: 1px solid var(--border); }

.btn-member-delete { background: transparent; border: 1px solid transparent; border-radius: var(--radius-sm); width: 32px; height: 32px; display: flex; align-items: center; justify-content: center; color: var(--text-muted); font-size: 16px; transition: background var(--transition), color var(--transition), border-color var(--transition); }
.btn-member-delete:hover { background: rgba(230,57,70,0.1); color: var(--red); border-color: rgba(230,57,70,0.25); }

.members-empty { display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 10px; padding: 60px 24px; color: var(--text-muted); text-align: center; }
.members-empty i { font-size: 48px; }
.members-empty p { font-size: 15px; font-weight: 600; color: var(--text-secondary); }
.members-empty span { font-size: 13px; }

.member-delete-modal { position: fixed; inset: 0; background: rgba(0,0,0,0.45); align-items: center; justify-content: center; z-index: 2000; }
.member-delete-modal__box { background: var(--bg-surface); border-radius: var(--radius-lg); padding: 32px 28px; max-width: 400px; width: 90%; box-shadow: 0 20px 60px rgba(0,0,0,0.2); text-align: center; animation: modalPop 0.2s ease; }
@keyframes modalPop { from { transform: scale(0.92); opacity: 0; } to { transform: scale(1); opacity: 1; } }
.member-delete-modal__icon { font-size: 40px; margin-bottom: 12px; }
.member-delete-modal__box h3 { font-size: 18px; font-weight: 700; margin-bottom: 10px; }
.member-delete-modal__box p { font-size: 14px; color: var(--text-secondary); line-height: 1.6; margin-bottom: 24px; }
.member-delete-modal__actions { display: flex; gap: 10px; justify-content: center; }

.btn-cancel { padding: 10px 24px; border-radius: var(--radius-sm); background: var(--bg-panel); border: 1px solid var(--border); color: var(--text-secondary); font-weight: 500; font-size: 14px; transition: background var(--transition); }
.btn-cancel:hover { background: var(--bg-hover); }
.btn-delete-confirm { padding: 10px 24px; border-radius: var(--radius-sm); background: var(--red); border: 1px solid var(--red); color: #fff; font-weight: 600; font-size: 14px; transition: opacity var(--transition); }
.btn-delete-confirm:hover { opacity: 0.88; }

/* ── 미디어 업로드 영역 (드래그 앤 드롭) ── */
.upload-zone {
    border: 2px dashed var(--border);
    border-radius: var(--radius-md);
    background: var(--bg-surface);
    padding: 40px 20px;
    text-align: center;
    cursor: pointer;
    transition: all var(--transition);
    color: var(--text-muted);
}
.upload-zone:hover {
    border-color: var(--purple-light);
    background: rgba(123,47,255,0.02);
}
.upload-zone.dragover {
    border-color: var(--purple);
    background: rgba(123,47,255,0.05);
    color: var(--purple);
}
.upload-zone i {
    font-size: 40px;
    margin-bottom: 12px;
    color: var(--text-muted);
    transition: color var(--transition);
}
.upload-zone.dragover i {
    color: var(--purple);
}
.upload-zone p {
    font-size: 14px;
    color: var(--text-secondary);
}

.media-preview-wrap {
    position: relative;
    margin-top: 16px;
    border-radius: var(--radius-md);
    overflow: hidden;
    background: #000;
    width: 100%;
    max-width: 400px;
}
.media-preview {
    width: 100%;
    display: flex;
}
.media-preview img,
.media-preview video {
    width: 100%;
    height: auto;
    display: block;
}
.btn-remove-media {
    position: absolute;
    top: 8px;
    right: 8px;
    width: 32px;
    height: 32px;
    background: rgba(0,0,0,0.6);
    color: #fff;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 18px;
    transition: background var(--transition);
}
.btn-remove-media:hover {
    background: var(--red);
}

/* ── 포트폴리오 다중 미디어 추가 영역 ── */
.upload-zone-mini {
    cursor: pointer;
    transition: all var(--transition);
    border: 2px dashed transparent;
}
.upload-zone-mini:hover {
    background: rgba(123,47,255,0.05);
    color: var(--purple);
}
.upload-zone-mini.dragover {
    border-color: var(--purple);
    background: rgba(123,47,255,0.1);
    color: var(--purple);
}
